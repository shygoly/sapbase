# 集成计划：next-shadcn-dashboard-starter → Speckit

## 📋 任务概述

将 `next-shadcn-dashboard-starter` 模板集成到 Speckit，同时严格遵守以下原则：

1. **Business-Agnostic** ✅ - 不引入任何业务假设
2. **Runtime-First** ✅ - 所有页面必须通过 Runtime 层
3. **Schema-First** ✅ - 先定义 Schema/Page Model，再写 UI

---

## ⚠️ 核心风险与对策

### 风险 1：诱惑直接写业务
**症状：** Table 写起来很快，Form 很好看，Demo 很爽 → 直接堆 JSX

**对策（铁律）：**
```
❌ 禁止：<Table><Form>
✅ 必须：<PageRuntime><CollectionRuntime><FormRuntime>
```

### 风险 2：跳过 Runtime 层
**症状：** shadcn/ui 太舒服，直接用组件库

**对策（强制）：**
- 所有页面必须有 `PageRuntime` 包装
- 所有表单必须有 `FormRuntime` 包装
- 所有列表必须有 `CollectionRuntime` 包装

---

## 📦 集成步骤

### Phase 1：清理模板（删除业务示例）

**删除以下目录（业务示例）：**
```
❌ features/products/        ← 产品管理示例
❌ features/kanban/          ← Kanban 示例
❌ features/auth/            ← 认证示例（与 core/auth 冲突）
```

**删除以下依赖（SaaS 特定）：**
```
❌ @clerk/nextjs             ← SaaS 认证库
❌ @dnd-kit/*                ← 拖拽库（Kanban 依赖）
❌ @sentry/nextjs            ← 可选，暂不集成
```

**保留以下目录（通用组件）：**
```
✅ components/ui/            ← 52 个 shadcn/ui 组件
✅ components/layout/        ← 布局组件
✅ components/forms/         ← 表单组件
✅ hooks/                    ← 通用 hooks
✅ lib/                      ← 工具函数
```

---

### Phase 2：升级依赖版本

**需要升级的依赖：**

| 依赖 | Speckit | 模板 | 操作 |
|------|---------|------|------|
| `next` | ^15.0.0 | ^16.0.7 | 升级到 ^16.0.7 |
| `tailwindcss` | ^3.4.0 | ^4.0.0 | 升级到 ^4.0.0 |
| `@radix-ui/*` | 较旧 | 最新 | 升级到最新 |

**新增依赖（模板独有）：**
```json
{
  "@tabler/icons-react": "^3.31.0",
  "@tanstack/react-table": "^8.21.2",
  "cmdk": "^1.1.1",
  "kbar": "^0.1.0-beta.45",
  "motion": "^11.17.0",
  "next-themes": "^0.4.6",
  "nextjs-toploader": "^3.7.15",
  "nuqs": "^2.4.1",
  "react-day-picker": "^8.10.1",
  "react-dropzone": "^14.3.5",
  "react-resizable-panels": "^2.1.7",
  "react-responsive": "^10.0.0",
  "recharts": "^2.15.1",
  "sharp": "^0.33.5",
  "sonner": "^1.7.1",
  "sort-by": "^1.2.0",
  "uuid": "^11.0.3",
  "vaul": "^1.1.2"
}
```

---

### Phase 3：合并文件结构

**目标结构：**
```
speckit/src/
├── core/                    ← Runtime 骨架（保持不变）
│   ├── auth/
│   ├── page-model/
│   ├── state-machine/
│   └── ...
├── features/                ← 业务特性（保持不变）
│   ├── admin/
│   └── ...
├── layouts/                 ← 布局组件（新增/合并）
│   ├── app-layout.tsx       ← 来自模板
│   ├── sidebar.tsx          ← 来自模板
│   └── ...
├── components/              ← UI 组件（合并）
│   ├── ui/                  ← 52 个 shadcn/ui 组件（来自模板）
│   ├── forms/               ← 表单组件（来自模板）
│   ├── layout/              ← 布局组件（来自模板）
│   └── ...
├── hooks/                   ← React hooks（合并）
│   ├── use-data-table.ts    ← 来自模板
│   ├── use-nav.ts           ← 来自模板
│   └── ...
├── lib/                     ← 工具函数（合并）
│   ├── data-table.ts        ← 来自模板
│   ├── format.ts            ← 来自模板
│   └── ...
└── app/                     ← Next.js 路由（保持不变）
```

---

### Phase 4：建立架构规则（防止陷阱）

**规则 1：Page Model First**
```typescript
// ❌ 禁止：直接写页面
export default function ProductsPage() {
  return <Table data={products} />;
}

// ✅ 必须：先定义 Page Model
const ProductsPageModel = {
  schema: ProductSchema,
  actions: {
    list: async () => { /* ... */ },
    create: async (data) => { /* ... */ },
  },
};

export default function ProductsPage() {
  return (
    <PageRuntime model={ProductsPageModel}>
      <CollectionRuntime />
    </PageRuntime>
  );
}
```

**规则 2：Runtime 包装强制**
```typescript
// ❌ 禁止：直接使用 UI 组件
<Form>
  <Input />
  <Button />
</Form>

// ✅ 必须：通过 Runtime 包装
<FormRuntime schema={schema}>
  <FormField name="email" />
  <FormSubmit />
</FormRuntime>
```

**规则 3：组件分层**
```
components/
├── ui/              ← 原始 shadcn/ui（无业务逻辑）
├── forms/           ← 表单组件（可有基础逻辑）
├── layout/          ← 布局组件（可有基础逻辑）
└── runtime/         ← Runtime 包装组件（业务逻辑）
```

---

### Phase 5：集成检查清单

- [ ] 删除业务示例目录（products, kanban, auth）
- [ ] 删除 SaaS 依赖（@clerk/nextjs, @dnd-kit/*）
- [ ] 升级 next 到 ^16.0.7
- [ ] 升级 tailwindcss 到 ^4.0.0
- [ ] 升级 @radix-ui/* 到最新版本
- [ ] 合并 components/ui/ 目录
- [ ] 合并 components/layout/ 目录
- [ ] 合并 components/forms/ 目录
- [ ] 合并 hooks/ 目录
- [ ] 合并 lib/ 目录
- [ ] 验证 TypeScript 编译无错误
- [ ] 验证 ESLint 无错误
- [ ] 运行测试套件
- [ ] 验证应用启动正常

---

## 🎯 成功标准

✅ **集成成功的标志：**

1. 所有 52 个 shadcn/ui 组件可用
2. 所有 13 个 hooks 可用
3. 所有工具函数可用
4. 没有业务示例代码
5. 没有 SaaS 依赖
6. TypeScript 编译无错误
7. 应用启动正常
8. 现有 admin 页面仍然正常工作

---

## ⚠️ 后续维护规则

**禁止事项：**
- ❌ 直接在 `components/ui/` 中添加业务逻辑
- ❌ 跳过 Runtime 层直接使用 UI 组件
- ❌ 在 `features/` 中添加 UI 示例
- ❌ 引入新的 SaaS 依赖

**必须事项：**
- ✅ 所有新页面必须有 Page Model
- ✅ 所有新表单必须有 FormRuntime
- ✅ 所有新列表必须有 CollectionRuntime
- ✅ 定期审查架构遵守情况

---

## 📝 实施顺序

1. **第一天：清理 + 依赖升级**
   - 删除业务示例
   - 删除 SaaS 依赖
   - 升级依赖版本
   - 验证编译

2. **第二天：文件合并**
   - 合并 components/
   - 合并 hooks/
   - 合并 lib/
   - 验证导入

3. **第三天：架构规则建立**
   - 创建 Runtime 包装组件
   - 建立 Page Model 示例
   - 文档化规则
   - 团队培训

---

## 🔗 相关文档

- Speckit Runtime 架构：`docs/runtime-architecture.md`
- Page Model 定义：`docs/page-model.md`
- 组件分层规则：`docs/component-hierarchy.md`
