# CRM 模块实体完整状态报告

## 📊 总结

**总实体数**: 8 个  
**有 Object Schema**: 7 个  
**有 State Machine 引用**: 2 个  
**有 State Machine 文件**: 1 个  
**有后端实体文件**: 1 个  
**有前端页面**: 4 个

## ❌ 标准 CRM 实体缺失

以下标准 CRM 实体**尚未创建**（在 wizard 中提到的示例实体）：

1. **Lead** (线索) - ❌ 不存在
2. **Account** (客户账户) - ❌ 不存在  
3. **Opportunity** (销售机会) - ❌ 不存在
4. **Contact** (联系人) - ❌ 不存在
5. **Contract** (合同) - ❌ 不存在
6. **Quote** (报价单) - ❌ 不存在

## 📋 现有实体详细状态

### ✅ 完整实体（已生成）

| 实体 | Schema | State Machine | SM 文件 | 后端实体 | 前端页面 | 状态字段 |
|------|--------|--------------|---------|----------|----------|----------|
| **NewObject** | ✅ | ✅ | ✅ | ✅ | ✅ | stage |

### ⚠️ 部分完成实体

| 实体 | Schema | State Machine | SM 文件 | 后端实体 | 前端页面 | 状态字段 | 问题 |
|------|--------|--------------|---------|----------|----------|----------|------|
| **Customer** | ✅ | ❌ | ❌ | ❌ | ✅ | status | 缺少后端实体和 state machine |
| **Order** | ✅ | ⚠️ | ❌ | ❌ | ✅ | status | stateMachine 是字符串而非引用，缺少后端实体和 SM 文件 |
| **OrderTracking** | ✅ | ❌ | ❌ | ❌ | ❌ | - | 缺少所有组件 |
| **FinancialTransaction** | ✅ | ❌ | ❌ | ❌ | ❌ | - | 缺少所有组件 |
| **Department** | ✅ | ❌ | ❌ | ❌ | ❌ | - | 缺少所有组件 |
| **User** | ✅ | ❌ | ❌ | ❌ | ❌ | - | 缺少所有组件 |
| **Transaction** | ❌ | ❌ | ❌ | ❌ | ✅ | - | 缺少 Schema |

## 🔧 需要修复的问题

### 1. Order 实体的 State Machine 格式错误

**问题**: `order.json` 中的 `stateMachine` 字段是 JSON 字符串，应该是引用字符串。

**当前**:
```json
"stateMachine": "{\"states\":[...]}"
```

**应该**:
```json
"stateMachine": "order-state-machine"
```

**需要**: 创建 `order-state-machine.json` 文件，并修复引用。

### 2. 缺少后端实体文件

以下实体需要生成后端实体文件：
- Customer
- Order
- OrderTracking
- FinancialTransaction
- Department
- User

### 3. 缺少 State Machine 文件

以下实体有 state machine 引用但缺少文件：
- Order (需要创建 `order-state-machine.json`)

以下实体应该有 state machine 但缺少：
- Customer (有 status 字段，应该转换为 state machine)
- OrderTracking (可能需要状态流转)
- FinancialTransaction (可能需要状态流转)

## 📝 建议的修复步骤

### 优先级 1: 修复 Order 的 State Machine

1. 提取 Order 的 stateMachine JSON 字符串
2. 创建 `speckit/public/specs/state-machines/order-state-machine.json`
3. 更新 `order.json` 中的 stateMachine 字段为引用

### 优先级 2: 为现有实体生成后端实体

使用代码生成器为以下实体生成后端实体：
- Customer
- Order  
- OrderTracking
- FinancialTransaction

### 优先级 3: 创建标准 CRM 实体

使用 AI Module Wizard 创建标准 CRM 实体：
- Lead (线索)
- Account (客户账户)
- Opportunity (销售机会)
- Contact (联系人)
- Contract (合同)
- Quote (报价单)

每个实体应该包括：
- Object Schema
- State Machine (如果适用)
- Backend Entity
- Frontend Pages (list, create, detail)
- Menu Item

## 🎯 完整 CRM 模块应该包含的实体

### 核心销售实体
1. **Lead** - 线索管理
2. **Account** - 客户账户
3. **Opportunity** - 销售机会
4. **Contact** - 联系人
5. **Quote** - 报价单
6. **Contract** - 合同

### 订单和交易实体
7. **Order** - 订单（已有，需修复）
8. **OrderTracking** - 订单跟踪（已有，需完善）
9. **FinancialTransaction** - 财务交易（已有，需完善）

### 客户管理实体
10. **Customer** - 客户（已有，需完善）

### 其他实体
11. **Department** - 部门（已有，需完善）
12. **User** - 用户（已有，需完善）

## ✅ 验证清单

- [ ] Order state machine 格式修复
- [ ] 所有现有实体都有后端实体文件
- [ ] 所有有状态字段的实体都有 state machine
- [ ] 所有 state machine 都有对应的文件
- [ ] 创建标准 CRM 实体（Lead, Account, Opportunity, Contact, Contract, Quote）
- [ ] 所有实体都有前端页面
- [ ] 所有实体都有菜单入口
