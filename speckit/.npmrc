# 本仓库统一使用 npm（根目录 package-lock.json 是唯一锁文件）。
#
# legacy-peer-deps=true：React 18 生态里部分依赖声明的 peer 范围尚未跟上，
# 不这样装会在 npm 7+ 直接报 ERESOLVE。
legacy-peer-deps=true

# 历史上这里还有 shamefully-hoist=true（pnpm 专有配置）与一段 pnpm 相关注释，
# 属于混用包管理器留下的残留 —— npm 不识别该键。详见 docs/PACKAGE_MANAGER.md。
